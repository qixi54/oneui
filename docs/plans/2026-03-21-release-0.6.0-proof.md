---
生成时间: 2026-03-21 16:58:00
参考文档: /opt/Oneflow/flowlab/项目/oneui/code/develop/docs/CHANGELOG-v0.6.0.md, /opt/Oneflow/flowlab/项目/oneui/code/develop/docs/PACKAGE-RELEASE-CHECKLIST.md, /opt/Oneflow/flowlab/项目/oneui/code/develop/package.json
角色定义: ARCH（架构师）
文档生成目的: 固化 OneUI v0.6.0 的发布前验证证据与外部 consumer smoke 结果
生成模型: GPT-5 Codex
---

# OneUI v0.6.0 Release Proof

## 发布对象

1. 包名：`@oneflowui/ui`
2. 版本：`0.6.0`
3. tarball：`oneflowui-ui-0.6.0.tgz`

## 仓库内门禁

以下命令均已通过：

1. `npm run type-check`
2. `npm run test`
3. `npm run build`
4. `npm run lint`
5. `npm run lint:style`

测试结果摘要：

1. `11` 个测试文件通过
2. `52` 个测试用例通过

构建结果摘要：

1. `dist/style.css` 约 `176.76 kB`
2. `vite build` 完成
3. `vite-plugin-dts` 声明生成完成

## npm 发版门禁

以下命令均已通过：

1. `npm whoami --registry=https://registry.npmjs.org`
   - 返回：`godotv`
2. `npm publish --dry-run --registry=https://registry.npmjs.org`
   - 结果：通过
3. `npm pack`
   - 结果：通过

tarball 摘要：

1. 文件名：`oneflowui-ui-0.6.0.tgz`
2. package size：约 `195.7 kB`
3. unpacked size：约 `896.3 kB`
4. total files：`434`

## dist 扫描

执行命令：

```bash
rg -n "/assets/|new Worker\\(new URL\\(|import.meta.url" dist
```

结果：

1. 未命中
2. 当前发布产物未发现绝对 `/assets/...` worker 路径
3. 当前发布产物未发现 `new Worker(new URL(..., import.meta.url))` 风险路径

## 外部 Consumer Smoke

### Vite 5.4.21

consumer 结果：

1. 安装本地 tarball 成功
2. `vite build` 通过
3. 产物主 JS chunk 约 `69.01 kB`
4. 样式产物约 `176.76 kB`

### Vite 8.0.1

consumer 结果：

1. 安装本地 tarball 成功
2. `vite build` 通过
3. 产物主 JS chunk 约 `69.01 kB`
4. 样式产物约 `173.77 kB`

## 结论

`v0.6.0` 已满足当前仓库的发布前门禁要求：

1. 仓库内基础门禁通过
2. npm 主站 dry-run 通过
3. tarball 打包通过
4. dist 风险扫描通过
5. 外部 consumer 在 Vite `5.4.21` 与 `8.0.1` 均构建通过
