---
生成时间: 2026-05-07 20:30:53
参考文档: /opt/Oneflow/flowlab/project/oneui/code/develop/docs/CHANGELOG-v0.9.8.md, /opt/Oneflow/flowlab/project/oneui/code/develop/docs/plans/2026-03-30-oneui-issues-136-138-remediation-plan.md
角色定义: ARCH
文档生成目的: OneUI 0.9.8 发布收口 proof 与可复核证据
生成模型: GPT-5 Codex
---

# OneUI v0.9.8 Release Proof

## 0) 当前状态

- Git 仓库：已提交并已推送到 `origin/main`
- npm 主站：未发布成功，当前可见版本仍为 `0.9.7`
- 阻塞类型：npm 账号/权限问题，不是代码构建或包内容问题

## 1) 发布基础事实

- package: `@oneflowui/ui`
- version: `0.9.8`
- registry: `https://registry.npmjs.org`
- npm whoami: `401 Unauthorized`
- npm view previous version: `0.9.7`
- npm view current version: `0.9.7`

## 2) 仓库内门禁

1. `npx vitest run src/tests/overlay.integration.spec.ts`
2. `npm run type-check`
3. `npm run build`

结果：

- `npx vitest run src/tests/overlay.integration.spec.ts` 通过
- `npm run test` 通过，`31/31` 文件、`136/136` 用例通过
- `npm run type-check` 通过
- `npm run build` 通过

## 3) 发包门禁

1. `npm publish --dry-run --access public --registry=https://registry.npmjs.org`
2. `npm pack --pack-destination <release-dir>`
3. `scripts/consumer-smoke-entrypoints.sh <release-dir> <tarball>`

结果：

- `npm publish --dry-run --access public --registry=https://registry.npmjs.org` 通过
- `npm pack --pack-destination /tmp/oneui_release_098` 通过
- tarball：`/tmp/oneui_release_098/oneflowui-ui-0.9.8.tgz`
- tarball details：`package size 280.2 kB`、`unpacked size 1.4 MB`、`total files 559`
- `rg -n '/assets/|new Worker\\(new URL\\(|import.meta.url' dist` 返回空结果
- `scripts/consumer-smoke-entrypoints.sh` 在外部 `npm install` 阶段长时间阻塞，未形成可复核通过日志

## 4) 实际发布

1. `npm publish --access public --registry=https://registry.npmjs.org`

结果：

- `git push origin main` 成功：`c9ea813..c129681  main -> main`
- `npm publish --access public --registry=https://registry.npmjs.org` 失败：
  `E404 Not Found - PUT https://registry.npmjs.org/@oneflowui%2fui`
- 结论：`0.9.8` 已进入仓库主线，但未进入 npm 主站；后续需要有效的 npm publish 权限后重试

## 5) 本轮变更焦点

1. `SidePanel` 支持独立配置 `lockScroll` 与 `trapFocus`
2. `useOverlay` 将 focus trap 与 scroll lock 解耦
3. release 入口文档与当前 npm 版本对齐
