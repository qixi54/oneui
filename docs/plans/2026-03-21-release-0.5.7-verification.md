---
生成时间: 2026-03-21 14:03:00
参考文档: /opt/Oneflow/flowlab/项目/oneui/code/develop/docs/plans/2026-03-21-release-0.5.7-plan.md
角色定义: ARCH（架构师）
文档生成目的: 记录 OneUI 0.5.7 补丁版本发布前验证结果
生成模型: GPT-5 Codex
---

# OneUI 0.5.7 验证报告

## 验证范围

本轮验证覆盖仓库内质量门禁、发包前检查、tarball 外部 consumer smoke，以及线上版本查询准备工作。

## 仓库内验证

以下命令均在仓库根目录执行并通过：

1. `pnpm lint`
2. `pnpm lint:style`
3. `pnpm type-check`
4. `pnpm test`
5. `pnpm build`

其中测试结果为 `11/11` files、`50/50` tests passed。

## 发包前检查

以下命令已通过：

1. `npm publish --dry-run --registry=https://registry.npmjs.org`
2. `npm pack`

本地 tarball 产物为：

- `oneflowui-ui-0.5.7.tgz`

## 外部 Consumer Smoke

按发布规则执行了双宿主矩阵验证：

1. `Vite 5.4.21` 根入口 named import smoke 通过
2. `Vite 8.0.1` `@oneflowui/ui/plugin` 子入口 smoke 通过

验证哨兵文件如下：

1. `/tmp/oneui-release-0.5.7.pvwVAT/vite5-js/build.ok`
2. `/tmp/oneui-release-0.5.7.pvwVAT/vite8-js/build.ok`

## 结论

`0.5.7` 已满足补丁版本发布前门禁要求，可执行 npm 主站发布与后续 release proof 收口。
