---
生成时间: 2026-03-21 15:36:07
参考文档: docs/plans/2026-03-21-release-0.5.9-plan.md
角色定义: ARCH
文档生成目的: 记录 OneUI 0.5.9 发布前验证结果
生成模型: GPT-5 Codex
---

# OneUI 0.5.9 Release Verification

## 验证命令

```bash
pnpm lint
pnpm lint:style
pnpm type-check
pnpm test
pnpm build
npm publish --dry-run --registry=https://registry.npmjs.org
npm pack
```

## 外部宿主验证

1. tarball 在 `Vite 5.4.21` root import consumer 中构建通过，主 chunk 约 `387.10 kB`
2. tarball 在 `Vite 8.0.1` plugin import consumer 中构建通过，主 chunk 约 `32.12 kB`，无 chunk size warning

## 实际验证结果

1. `pnpm lint` 通过
2. `pnpm lint:style` 通过
3. `pnpm type-check` 通过
4. `pnpm test` 通过：`11/11` files，`52/52` tests
5. `pnpm build` 通过
6. `npm publish --dry-run --registry=https://registry.npmjs.org` 通过
7. `npm pack` 通过，产物为 `oneflowui-ui-0.5.9.tgz`

## 验证结论

`0.5.9` 发布前验证通过后，可作为当前 OneUI 推荐补丁版本，覆盖 `0.5.8` 之后的 package boundary 治理与完整治理收口状态。
